<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:clickable="true"
    android:foreground="?attr/selectableItemBackground"
    android:paddingLeft="@dimen/activity_margin"
    android:paddingRight="@dimen/activity_margin"
    android:paddingTop="@dimen/dimen_10">

    <TextView
        android:id="@+id/idrow_medicament_id_dot"
        android:layout_width="wrap_content"
        android:layout_height="@dimen/dot_height"
        android:layout_marginRight="@dimen/dot_margin_right"
        android:includeFontPadding="false"
        android:textColor="@color/colorPrimary"
        android:textSize="14sp" />

    <TextView
        android:id="@+id/idrow_medicament_denumire"
        android:layout_width="match_parent"
        android:layout_marginTop="@dimen/dot_margin_right"
        android:layout_height="wrap_content"
        android:layout_toRightOf="@id/idrow_medicament_id_dot"
        android:textColor="@color/darker_grey"
        android:textSize="@dimen/note_list_text" />
</RelativeLayout>